{
    "name": "stories",
    "license": "ISC",
    "scripts": {
        "build": "parcel build sass/style.scss --out-dir static/styles --no-source-maps",
        "watch": "parcel watch sass/style.scss --out-dir static/styles --no-hmr --no-source-maps"
    },
    "dependencies": {
        "bulma": "^0.9.1",
        "parcel-bundler": "^1.12.4"
    },
    "devDependencies": {
        "sass": "^1.32.4"
    }
}
